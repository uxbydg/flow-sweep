# Transcript Sweep

A concept for Wispr Flow's history view, built as a design-engineering artifact for the Wispr Flow
Design Engineer application. One screen, one action: find the transcript entries that are almost
certainly accidents, show them with the reason and a confidence, sweep them in one motion, and keep
them somewhere they can come back from. Built against one real user's 874 entries.

**Stack:** React 19, TypeScript, Vite, Motion, Lucide, SCSS modules. First React build, AI-paired.

---

## Resume here (read this first in a fresh session)

**State (14 Sep, evening):** the Flow-look rebuild is WIRED and runs end to end on the real history.
Seven local commits, no remote, nothing pushed. The 14 Sep session crashed mid-refactor (components
written, App not rewired); 853b5dc is the recovery commit.

```bash
cd ~/flow-sweep && npm run dev -- --port 5179     # http://localhost:5179
node scripts/verify.ts                              # classifier counts against the real export
```

Demo switches: `?demo=reminder` seeds a mixed batch (8 Empty, 3 Cut off, 1 flagged) that leaves
tonight so the reminder card shows; `?day=N` moves the clock N days forward. Swept state lives in
localStorage (`flow-sweep:swept`, `flow-sweep:gone`); clear it to reset.

**Context to load, nothing else:** this README, `SPEC.md`,
`~/job-search/wispr-flow-design-engineer/artifact-plan.md` (the one-day plan, Block 0 numbers, and the
progress log at the bottom).

### Waiting on Daniel
1. **Look at it.** Every state in SPEC.md is built: History (day cards, sticky bar, broom), Summary,
   Review (section-level select, Retry first, Short replies collapsed), Sweep, the holding cell inside
   History (grouped by the day rows leave; Restore, Restore all, Empty now + confirm), and the
   leaving-today reminder card under the stats.
2. **Say go on the public repo + Vercel.** One push, per the standing rule.

### Definition of done
A stranger grades it in two minutes from one URL and one paragraph: (1) the public build runs the
whole loop on sample data with no instructions and no console errors; (2) it survives keyboard-only,
390 px, reduced motion, ships nothing of Flow's and nothing private; (3) this README is the write-up;
(4) essay paragraph, CLAIMS.md row, resume link exist. Then one push. Not in scope: threshold slider,
wiring the drawn menu items, mobile history, dark mode. Triage for new notes: does it change what
the stranger sees in two minutes?

### Next steps, in order
1. **390 px pass.** The CSS exists (`@media (max-width: 760px)`) but is unverified: Chrome would not
   resize below its minimum in the extension. Check with devtools device mode.
2. Keyboard: rows are not yet focusable (Tab through rows in History/cell). Space/Enter/Escape work.
3. Motion polish: sweep exit stagger is in; restore is not yet its exact mirror. Reduced-motion is
   opacity only via the global rule.
4. Stats card: it computes 54.9K words from the export; Flow's own Insights says 100.8K. Decide whether
   the card shows the export's truth or Flow's number.
5. This README becomes the write-up (see "What the data said" below, keep it).
6. Public GitHub repo, Vercel project, **one push**. Public build uses `src/data/history.sample.json`
   (synthetic, matches the real distribution); the real export is gitignored and must stay so.
7. Essay paragraph for "exceptional ability", CLAIMS.md row ("first React/TS build, AI-paired, one
   day"), tick the to-dos in `notes.md`.

### Cut list, in order, if time runs short
Motion polish → CSS transitions. Flag phrases → static list.
Never cut: review before sweep, the holding cell with restore, real data locally.

---

## Where things are

```
SPEC.md                      the one-page spec (categories, confidence model, four states, motion)
src/sweep/detect.ts          classifier: empty / short / flagged, app-weighted confidence
src/data/load.ts             prefers history.private.json (gitignored) else history.sample.json
src/data/history.sample.json synthetic public dataset, generated by scripts/make-sample.mjs
src/App.tsx                  state + wiring: History or holding cell in place, reminder, dialogs
src/components/              Chrome (titlebar, sidebar, banner, stats), HistoryList, HoldingCell,
                             Reminder, Dialogs (SweepDialog summary→review, Confirm)
src/sweep/holding.ts         7-day hold: clock (?day, ?demo), expiry, localStorage for swept/gone
src/sweep/labels.ts          section names, order, and which sections start unselected
src/App.module.scss          styles; src/styles/tokens.scss holds the tokens sampled from Flow (light only)
scripts/verify.ts            counts against the real export (node scripts/verify.ts)
ref/                         Flow screenshots for the visual pass (Daniel adds these)
```

## What the data said (from Flow's own local store, read-only)

874 entries, 8 Aug to 11 Sep 2026. **100 empty (11.4%), 83 under 20 characters (9.5%), 1 flagged
in the entry itself. 184 candidates, about 1 in 5. 145 preselected at 0.60.**

- **Empties are not brief hotkey taps.** They average 8.5 s of recording (max 152 s): recordings
  that returned no text. The taps are the under-20 set (2.6 s recorded, 1.6 s of speech). Two
  failures, two upstream causes; the store does not say why the text did not come back.
- **The retry pattern occurs once** in 874. Dropped.
- **Context decides what an accident is.** "Hi." into Messages is a text; "Hi." into Terminal is not.
  Confidence is weighted by the target app. This is the design in the project.
- **Storage is not the value.** All audio is 597 MB; the under-20 set carries 16 MB. The sweep buys a
  legible history, not disk.
- Prior art: Flow deletes one transcript at a time via a three-dot menu, offers auto-delete after
  24 h, and its docs say "deleted transcripts cannot be recovered." No bulk review, no undo. The
  Swept place is the answer to that sentence.

## Rules
- `src/data/history.private.json` is Daniel's real dictation history. It is gitignored. It never
  enters a public repo, a deploy, or a screenshot that leaves this machine.
- Flow's live database is never opened. Discovery used Flow's own backup copy, read-only.
- Commit freely, **push once** at the end of the build day.
